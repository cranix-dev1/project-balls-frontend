REACT_APP_SOCKET_URL=wss://ix.nickyboi.com
# Sets the WebSocket URL your React app will use to connect to the backend.
# The 'REACT_APP_' prefix is required for create-react-app to inject this variable at build time.

JWT_SECRET=some_very_secret_key
# Sets the secret key used by your backend to sign and verify JWT tokens.
# This should be kept private and is used for authentication/security.
